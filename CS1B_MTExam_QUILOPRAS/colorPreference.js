let colors = [red, blue, yellow];

function additional_color(newColor) {
    colors.push(newColor); // adding new color to the array
    console.log(colors); // logging the array or the contents of the array after pushing
  }
  
  // adding colors
  additional_color("red-orange");
  additional_color("blue-green");
  additional_color("yellow-green");